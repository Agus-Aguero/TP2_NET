@model IEnumerable<Academia.Entities.materias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.planes.desc_plan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.desc_materia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hs_semanales)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hs_totales)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.planes.desc_plan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.desc_materia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.hs_semanales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.hs_totales)
            </td>
            <td>
                @Html.ActionLink("Inscribirme", "Cursos", new { idMateria = item.id_materia }) |
                @Html.ActionLink("Details", "Details", new { id = item.id_materia }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id_materia })
            </td>
        </tr>
    }

</table>
